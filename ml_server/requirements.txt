# Orafinite ML Sidecar Dependencies (GPU-ONLY / CUDA 12.1)
# This sidecar REQUIRES an NVIDIA CUDA-capable GPU.
# CPU/ONNX mode is not supported — do NOT add optimum or onnxruntime here.

--extra-index-url https://download.pytorch.org/whl/cu121

# gRPC
grpcio==1.60.0
grpcio-tools==1.60.0
protobuf==4.25.2

# LLM Guard - Real-time protection
llm-guard==0.3.14

# Garak - Vulnerability scanning
garak==0.9.0.13

# ML/AI (CUDA-enabled via --extra-index-url above)
torch>=2.0.0
transformers>=4.36.0
numpy>=1.24.0

# Async support
asyncio
aiofiles

# HTTP client (used by CustomRESTGenerator for custom API endpoints)
requests>=2.31.0

# Utilities
python-dotenv==1.0.0
pydantic==2.5.3
loguru==0.7.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
